[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "three_card_poker"
version = "0.1.0"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/three_card_poker"]